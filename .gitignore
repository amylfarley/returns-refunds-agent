# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Configuration Files (SENSITIVE - DO NOT COMMIT)
# Actual config files are excluded, but .example files are safe to commit
cognito_config.json
memory_config.json
gateway_config.json
gateway_role_config.json
lambda_config.json
runtime_config.json
runtime_execution_role_config.json

# Example config files are safe to commit (*.example files are allowed)
# These help users understand the structure without exposing sensitive data

# Keep kb_config.json as it's from CloudFormation and not sensitive
# kb_config.json can be committed if needed

# AWS
.aws/

# Logs
*.log

# Environment variables
.env
.env.local
